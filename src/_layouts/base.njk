{# Base HTML, compatible with what Vite expects #}
<!DOCTYPE html>
<html lang="en">
{% include "head.njk" %}
<body>
   {% include "header.html" %}

    {{ content | safe }}

    {% include "banner.html" %}

    <footer class="px-4 py-6 border-t-2 border-gray-200 bg-gray-50 sm:px-10">
        <p class="text-xs leading-5 text-gray-500 text-center">
            &copy; {% year %} {{ meta.siteName }}{% if meta.authorName %} • {{ meta.authorName }}{% endif %}
        </p>
    </footer>
</body>
</html>